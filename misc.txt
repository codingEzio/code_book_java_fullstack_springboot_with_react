docker pull mariadb:10.7.4-focal
brew install mariadb

mysql.server start
brew services start mariadb

mysql
sudo mysql -u root

# if anything goes wrong, run commands below
./mvnw clean
./mvnw spring-boot:run
